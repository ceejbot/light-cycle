/*global describe:true, it:true, before:true, after:true */

var
    chai   = require('chai'),
    assert = chai.assert,
    expect = chai.expect,
    should = chai.should()
    ;

describe('light-cycle', function()
{
    it('should someday have tests');
});
